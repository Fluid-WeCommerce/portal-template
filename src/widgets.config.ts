/**
 * Remote DOM widget package source.
 *
 * `fluid portal widget create <name>` adds authoring definitions here. The CLI
 * builds and publishes them; the portal host discovers the resulting canonical
 * package descriptors from the authenticated widget catalog.
 */
export const widgetPackages = [] as const;
